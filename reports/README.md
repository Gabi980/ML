# Reports

After running `python -m src.train`, this folder will contain:

- `tables/`: CSV files with statistical summaries, missing values, model comparisons;
- `figures/`: EDA plots;
- `project_summary.md`: generated project summary.
